# WINE AI agent workflow entrypoint

Read `AGENTS.md`, `docs/agent-context/AGENT_WORKFLOW.md`, and
`.agents/CHECKPOINT.md` at mission start.

The workflow document is the only source of truth for execution, autonomy,
verification, fan-out, cost, and stop rules. This entrypoint intentionally does
not restate them.
