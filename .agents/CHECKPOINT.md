# Mission checkpoint

- goal: Add a general Useful Answer Recovery layer to the answer path so that when a literal answer is insufficiently supported, the system recovers to the closest supported useful answer with preserved uncertainty — without weakening the anti-hallucination gate. Follow-up: finalize the recovery acceptance branch (feature/recovery-acceptance, PR #64) and record the answerability grader outage as a separated production follow-up.
- completed: Recovery layer implemented and verified on branch `feature/useful-answer-recovery`, committed as 49e20c8, pushed, PR #63 merged (merge commit bd6bd97). New module `src/knowledge/usefulRecovery.js` implements `attemptRecovery` (strategies: partial_evidence, nearest_confirmed_fact, preference_discovery, entity_alternatives, honest_limitation) with provenance-carrying candidates, web/low-confidence/mode-level filtering, and discovery that skips the answerability grader. Integrated into `src/tools/searchLayeredKnowledge.js` (not_found + insufficient branches now recover, status `recovered`), `src/knowledge/knowledgeOrchestrator.js` (adds `recovery` block), and `src/evaluation/textKnowledgeEvaluation.js` (recovery-aware prompt + result). `layeredRouter` gained a `skipAnswerability` hand-off for discovery. `answerable` is never flipped to true; general-knowledge and confirmed answers are unchanged. Acceptance branch `feature/recovery-acceptance` (commit 917ad73) adds `scripts/recovery-acceptance.js` + 22-row RU/RO/EN harness (benchmark-results/recovery-local.json, recovery-2026-08-14.json): useful_recovery_rate 1.0, dead_end 0, internal-language violations 0, unsupported claims 0, controls untouched, all gates green; PARTIAL_EVIDENCE now requires the grader `match` verdict so unattributable evidence recovers as `entity_alternatives`. Two end-to-end contract pins re-pinned to the verified acceptance contract. `checkMissingLocalImports` passes. Pre-existing unrelated failures reproduced on a clean checkout: dashboardBargeIn, entityResolver, knowledgeChunksMigration, plus answerabilityClaimClass/layeredKnowledgeToolAnswerabilityContract (re-pinned, now green).
- decisions: Reuse the existing Phase 1 orchestrator, provenance, answer modes, and entity relations; do not create a second answer pipeline. Behavior changes belong to the answer/recovery layer, not Voice Style. Recover only `unknown exact answer → supported adjacent answer`; never `unknown → plausible invention`. Discovery is corpus-only (no web) and skips the LLM answerability grade; `answerable` stays tri-state and unchanged. Partial evidence is only legal with the grader's `match` verdict.
- blockers: none for the recovery mission.
- production follow-ups: answerability LLM grader outage — `answerability_check_error`/`answerability_check_unavailable` observed (all 22 acceptance rows grader-down during local run; pre-existing benchmark notes the same). The deterministic safe path covers the stalled-grader case structurally, but the grader must be re-validated against production behavior; investigate as a separate task if the outage continues to affect production answers.
- next_action: confirm PR #65 (restored Phase 6 Wine Intelligence layer) CI gates green and merge; then, as the core Wine Intelligence mission, wire `src/knowledge/wineIntelligence.js` `runInference` into the live answer path gated by mode `allowInference` (expert mode only), extend the Answer Audit benchmark with the four Phase 6 scenarios, and re-validate the answerability grader against production.