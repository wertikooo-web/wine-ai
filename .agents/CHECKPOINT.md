# Mission checkpoint

- goal: Complete Phase 6 Wine Intelligence for the ordinary production user path. Currently intelligence is wired only in expert mode (`allowInference: true`, PR #66), so real users on the default `knowledge_web` mode never get recommendations/pairings/comparisons/routes. Fix the gating architecturally (intent-driven, not mode-flag), broaden intent detection to natural phrasings ("Хочу мягкое красное", "Что взять к утке?", "Cricova или Purcari для дегустации?"), add a Phase 6 acceptance benchmark on the same audit path as the live tool, and verify ≥8 real scenarios end-to-end through the production tool path.
- completed: Gating fixed in `src/tools/searchLayeredKnowledge.js`: inference now runs via `inferForQuestion` gated by `detectScenario` INTENT (any mode, including default knowledge_web), never by `policy.allowInference`. The `no_inference` audit constraint still suppresses it. `src/knowledge/wineIntelligence.js` gained `inferForQuestion` (shared by tool + orchestrator so the benchmark measures production behavior), `allowCatalog` threading, and broadened detection (descriptor+intent-verb recommendation, duck/lamb/steak pairing phrasings, two-entity + или/or comparison, route "спланируй винный тур"). `src/knowledge/knowledgeOrchestrator.js` attaches the same intent-gated `inference` block and honors `no_inference`; `src/knowledge/answerAudit.js` threads constraints. Voice-safe `INFERENCE_GUIDANCE` appended to `final_instruction` in both tool and orchestrator so the model speaks the recommendation in the sommelier voice and never narrates inference internals. New `scripts/phase6-acceptance.js` + 14-row `tests/benchmark/phase6-dataset.json` (`npm run acceptance:phase6`): intent_attachment_rate 1.0, recommendation_success_rate 1.0 (8/8), hard-color violations 0, unsupported claims 0, honesty failures 0, negative controls untouched, no_inference honored, internal-language violations 0 — PASS. Tool-path E2E evidence for 9 scenarios recorded in `benchmark-results/phase6-tool-e2e.json` (status, inference block, voice-safe guidance) and audit-path results in `benchmark-results/phase6-local.json`. recovery-acceptance still PASSES (useful_recovery_rate 1.0) with inference attached. wineIntelligence/answerModes/knowledgeOrchestrator/answerAudit/auditMetrics/tools/layeredKnowledgeTool*/pairingEngine/checkMissingLocalImports tests green. `npm run lint` green. Pre-existing runner failures unchanged from `main` baseline (dashboardBargeIn fails standalone on main; entityResolver + knowledgeChunksMigration pass standalone on both branches and fail only inside the shared runner — identical on main, unrelated to this diff).
- decisions: Inference is gated by Phase 6 INTENT at runtime, not by answer_mode — the mode flag `allowInference` remains a declared capability (admin listing / no_inference audit contract) but is no longer the runtime gate. Factual turns ("сколько стоит вино Cricova 1952", "расскажи о виноделии") never attach inference. All user-facing inference text stays in the sommelier voice (existing label tables; no RAG/database/search narration). Route planning needs relation edges (Postgres, seeded in production); locally relations are empty so route rows resolve as honest `found:false` with `missing` — the benchmark gates route rows on the honesty contract (never fabricated stops). Benchmark follows the recovery-acceptance pattern (same `answerAudit.runAnswerAudit` path), not a parallel framework.
- blockers: none. Note: local Postgres is absent (`db.getPool()` null), so relation-dependent route success and the answerability LLM grader cannot be exercised locally (same condition as the prior recovery mission); both are production/CI evidence, not blockers.
- production_state: unchanged/not inspected — branch `feat/phase6-production-completion`, uncommitted, not deployed.
- next_action: independent verifier verdict on the diff + test/benchmark evidence, then commit, PR, merge after green required gates, deploy to production, and re-run `acceptance:phase6` against the live HTTP endpoint (`AUDIT_BASE_URL`) to confirm the ordinary user path in production.
