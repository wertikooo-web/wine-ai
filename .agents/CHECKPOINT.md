# Mission checkpoint

- goal: Add a general Useful Answer Recovery layer to the answer path so that when a literal answer is insufficiently supported, the system recovers to the closest supported useful answer with preserved uncertainty — without weakening the anti-hallucination gate.
- completed: Recovery layer implemented and verified on branch `feature/useful-answer-recovery`, committed as 49e20c8, pushed, PR #63 open (https://github.com/wertikooo-web/wine-ai/pull/63). New module `src/knowledge/usefulRecovery.js` implements `attemptRecovery` (strategies: partial_evidence, nearest_confirmed_fact, preference_discovery, entity_alternatives, honest_limitation) with provenance-carrying candidates, web/low-confidence/mode-level filtering, and discovery that skips the answerability grader. Integrated into `src/tools/searchLayeredKnowledge.js` (not_found + insufficient branches now recover, status `recovered`), `src/knowledge/knowledgeOrchestrator.js` (adds `recovery` block), and `src/evaluation/textKnowledgeEvaluation.js` (recovery-aware prompt + result). `layeredRouter` gained a `skipAnswerability` hand-off for discovery. `answerable` is never flipped to true; general-knowledge and confirmed answers are unchanged. Tests: new `tests/usefulRecovery.test.js` (incl. an end-to-end createImpl case) plus updated pins in `tests/answerabilityClaimClass.test.js` and `tests/layeredKnowledgeToolAnswerabilityContract.test.js`. All 10 recovery-related suites pass; `checkMissingLocalImports` now passes (file tracked). Independent verifier returned `verdict: pass` (blockers: none); its two non-blocking points (discovery re-grading; hardcoded allowCatalog) were fixed. Pre-existing unrelated failures reproduced on a clean checkout: dashboardBargeIn, entityResolver, knowledgeChunksMigration.
- decisions: Reuse the existing Phase 1 orchestrator, provenance, answer modes, and entity relations; do not create a second answer pipeline. Behavior changes belong to the answer/recovery layer, not Voice Style. Recover only `unknown exact answer → supported adjacent answer`; never `unknown → plausible invention`. Discovery is corpus-only (no web) and skips the LLM answerability grade; `answerable` stays tri-state and unchanged.
- blockers: none.
- production_state: not inspected this mission.
- next_action: confirm PR #63 required CI gates are green (merge deferred — not authorized), then inspect production state per verification rules.