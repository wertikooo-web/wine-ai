# Mission checkpoint

- goal: Complete Phase 6 Wine Intelligence for the ordinary production user path. Currently intelligence is wired only in expert mode (`allowInference: true`, PR #66), so real users on the default `knowledge_web` mode never get recommendations/pairings/comparisons/routes. Fix the gating architecturally (intent-driven, not mode-flag), broaden intent detection to natural phrasings ("Хочу мягкое красное", "Что взять к утке?", "Cricova или Purcari для дегустации?"), add a Phase 6 acceptance benchmark on the same audit path as the live tool, and verify ≥8 real scenarios end-to-end through the production tool path.
- completed: Base (PR #67) done: inference now runs intent-gated via `inferForQuestion` (no mode flag) in both `src/tools/searchLayeredKnowledge.js` and `src/knowledge/knowledgeOrchestrator.js`, with `no_inference` suppression and voice-safe `INFERENCE_GUIDANCE`. Verifier round done: (1) recovery-vs-inference conflict resolved — `attemptRecovery` is skipped whenever the inference block is the authoritative answer (`inference.found === true`); when inference honestly found nothing, recovery may still supply closest supported facts (preserves the answerability-recovery contract, not a competing answer). (2) Zero-data comparison is now honest: `compareWines` returns `found:false` when either candidate lacks a single supported fact and names exactly which side is missing data; no fabricated tables. (3) Catalog editorial articles are never recommended as wines: `isWineCatalogItem` filters catalog rows to real bottle signals (wine entity, vintage, price, or classified product URL). Hard color gate (conflicting-colour wine never recommended when a colour is named), discovery-recommend detection fixed ("Что необычное можно попробовать из молдавского?"), answerability contract test updated for authoritative-inference. Verifier round 2 (independent, release-candidate gate): **3 blockers found and fixed** — (B1) factual attribute asks carrying recommendation vocab ("Подскажи цену на красное вино Cricova 1952", "Хочу узнать цену красного вина Cricova", "какая крепость у вина Cricova?", "сколько стоит Negru de Purcari?") now return `null` in `detectScenario` via new `FACTUAL_ATTRIBUTE_ASK_RE` instead of `recommend_wine`; (B2) education comparisons ("Чем отличается красное вино от белого?", "разница между сухим и полусухим") carry no registry entity so `COMPARISON_RE` now requires `_mentionsRegistryEntity` — they stay general knowledge instead of hijacking `compare_wines`; (B3) `wineFacts` no longer uses `findWineStyle` color-substring fallback — new `groundedWineStyle` resolves style only from real profiles or exact style/grape name, and `compareWines.hasFacts` no longer counts a bare producer relation, so a comparison with no supported wine attributes is honest `found:false`. Acceptance: 11/11 gates PASS on 19 rows (12 intent + 6 negative controls + no_inference constraint); intent_attachment_rate 1.0, recommendation_success_rate 1.0 (6/6), 0 honesty/negative-control/no_inference/recovery-conflict/compare-data/ungrounded-wine failures; evidence at `benchmark-results/phase6-2026-08-16.json`. Unit: wineIntelligence, layeredKnowledgeToolAnswerabilityContract, knowledgeOrchestrator, usefulRecovery, answerAudit, answerabilityClaimClass, layeredKnowledgeToolContract, layeredKnowledgeRouter, pairingEngine all PASS.
- decisions: Inference is gated by Phase 6 INTENT at runtime, not by answer_mode — the mode flag `allowInference` remains a declared capability (admin listing / no_inference audit contract) but is no longer the runtime gate. Factual turns ("сколько стоит вино Cricova 1952", "расскажи о виноделии", attribute asks with intent vocab) never attach inference. A comparison scenario requires at least one concrete registry entity; education category comparisons stay general knowledge. Wine style is only ever resolved from grounded profiles (catalog/official/exact name), never from a colour substring. A bare producer relation is not a supported wine attribute for comparison sufficiency. All user-facing inference text stays in the sommelier voice (existing label tables; no RAG/database/search narration). Route planning needs relation edges (Postgres, seeded in production); locally relations are empty so route rows resolve as honest `found:false` with `missing` — the benchmark gates route rows on the honesty contract (never fabricated stops). Benchmark follows the recovery-acceptance pattern (same `answerAudit.runAnswerAudit` path), not a parallel framework.
- blockers: none. Note: local Postgres is absent (`db.getPool()` null), so relation-dependent route success and the answerability LLM grader cannot be exercised locally (same condition as prior missions); both are production/CI evidence, not blockers. Pre-existing unrelated failure on `main`: `searchWineKnowledgeFallback.test.js` fails standalone on the clean baseline too — not caused by this mission.
- production_state: unchanged/not inspected — branch `main` (after PR #67 merge), uncommitted fix, not deployed.
- next_action: independent verifier verdict on the blocker-fix diff + test/benchmark evidence, then commit, PR, merge after green required gates, deploy to production, and re-run `acceptance:phase6` against the live HTTP endpoint (`AUDIT_BASE_URL`) to confirm the ordinary user path in production.
