# WineMD Rive Implementation Report — <date>

> Use this exact structure at the end of every work session, per `SKILL.md`'s "Hard rules". Fill in real content; delete this blockquote.

## What was created
- (new files, full paths)

## What was changed
- (existing files touched, full paths, one line on why each)

## Validator results
```
$ node scripts/validate-rive-manifest.mjs
<paste real output>

$ node scripts/validate-visual-events.mjs <path>
<paste real output>

$ node scripts/validate-character-assets.mjs
<paste real output>
```

## What remains unverified or blocked
- (anything requiring the Rive editor, an artist, or a browser test that wasn't done this session — be specific, don't say "mostly done")

## Known spec gaps touched or left open
- (reference `references/troubleshooting.md` items by number if relevant)

## Exact next step
- (the one concrete next action — e.g. "open <file> in Rive editor and verify state machine transitions" or "wait for layered PSD from artist before continuing Phase 1 rig work")
