# Prompt: build Rive rig
Act as a senior Rive technical animator. Import the approved layered assets and build the `Sommelier` artboard using the manifest, state-machine and constraints documents. Create MVP first: Idle, Blink, Speaking, Present Wine. Validate deformation before adding more states. Export `winemd-sommelier.riv` and a browser preview.
