# Next.js example outline
Load `winemd-sommelier.riv`, retrieve `SommelierSM` inputs, implement `RiveInputAdapter`, subscribe to WineMD visual events, and call `SommelierController.apply`. Binary `.riv` is not included until built and reviewed in Rive.
