# Export rules
- PSD, RGB, transparent background.
- Each deformable part on a separate layer.
- PNG exports: RGBA, identical canvas size, no trimming.
- SVG only for clean vector props.
- Use snake_case names from the specification.
