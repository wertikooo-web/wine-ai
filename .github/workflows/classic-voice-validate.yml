name: Classic voice validate

on:
  push:
    branches:
      - feat/classic-stt-llm-tts
  workflow_dispatch:

permissions:
  contents: write

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: feat/classic-stt-llm-tts
          fetch-depth: 0

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - run: npm ci
      - run: node scripts/finalize-classic-ui.js
      - run: npm test
      - run: npm run check:missing-imports

      - name: Commit generated dashboard integration
        run: |
          if git diff --quiet; then
            echo "No generated changes"
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add public/dashboard.html
          git commit -m "feat(voice): connect engine-family UI to dashboard"
          git push
