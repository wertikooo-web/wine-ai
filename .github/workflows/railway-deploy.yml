name: Deploy Railway Production

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

concurrency:
  group: railway-production-deploy-${{ github.ref }}
  cancel-in-progress: false

jobs:
  railway-auth-diagnostic:
    if: github.event_name == 'pull_request'
    runs-on: ubuntu-latest
    timeout-minutes: 10
    environment: production
    steps:
      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: Install Railway CLI
        run: npm install --global @railway/cli

      - name: Validate Railway token and project visibility
        env:
          RAILWAY_TOKEN: ${{ secrets.RAILWAY_TOKEN }}
        run: |
          set -euo pipefail
          test -n "$RAILWAY_TOKEN" || { echo "RAILWAY_TOKEN secret is missing"; exit 1; }
          railway whoami
          railway status --service wine-ai-realtime --environment production

  deploy:
    if: github.event_name != 'pull_request'
    runs-on: ubuntu-latest
    timeout-minutes: 20
    environment: production
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'npm'

      - name: Install Railway CLI
        run: npm install --global @railway/cli

      - name: Validate Railway credentials
        env:
          RAILWAY_TOKEN: ${{ secrets.RAILWAY_TOKEN }}
        run: |
          set -euo pipefail
          test -n "$RAILWAY_TOKEN" || { echo "RAILWAY_TOKEN secret is missing"; exit 1; }
          railway whoami
          railway status --service wine-ai-realtime --environment production

      - name: Deploy wine-ai-realtime
        env:
          RAILWAY_TOKEN: ${{ secrets.RAILWAY_TOKEN }}
        run: |
          set -euo pipefail
          railway up \
            --service wine-ai-realtime \
            --environment production \
            --detach \
            --no-gitignore

      - name: Wait for deployed service
        run: |
          set -euo pipefail
          BASE_URL="https://wine-ai-realtime-production.up.railway.app"
          for attempt in $(seq 1 90); do
            code="$(curl -sS -L --max-time 20 -o /tmp/health.json -w '%{http_code}' "$BASE_URL/health" || true)"
            echo "attempt=$attempt http=$code"
            if [ "$code" = "200" ]; then
              cat /tmp/health.json
              exit 0
            fi
            sleep 10
          done
          echo "Production did not become healthy after deploy"
          exit 1
